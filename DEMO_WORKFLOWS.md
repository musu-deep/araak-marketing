# المشاريع التجريبية للمقياس الزمني

تضيف Migration التالية أربعة مشروعات تجريبية مرتبطة بمسارات زمنية كاملة:

```text
20260728223000_add_odoo_member_links_and_demo_workflows.sql
```

## السيناريوهات

1. **مجمع إداري متكامل — الرياض**
   - يسير ضمن الخطة.
   - الدراسة الفنية وحصر البنود مكتملان.
   - عروض الأسعار عند 65%.

2. **تأهيل وتوسعة مبنى صحي — جدة**
   - مشروع متعثر.
   - مرحلة عروض الموردين عند 35% ومتأخرة عن وقتها المخطط.
   - يظهر سبب التأخير وموضع الاختناق بوضوح.

3. **تطوير مستودع مركزي ذكي — المدينة**
   - الدراسة والتسعير مكتملان.
   - الملف في مراجعة واعتماد مسؤول المشاريع عند 75%.

4. **إنشاء مركز تدريب مهني — الدمام**
   - مشروع عاجل وقريب من الإغلاق.
   - الرفع النهائي عند 85%.
   - يوضح أثر هامش الأمان قبل موعد التسليم.

## الارتباط بـ Odoo

المشروعات التجريبية لا تنشئ سجلات وهمية داخل قاعدة Odoo الإنتاجية. بدلًا من ذلك:

- كل مرحلة تُسند إلى عضو المنصة حسب `role_key`.
- وظيفة `platform-team` تطابق العضو مع `hr.employee` في Odoo.
- تحفظ المنصة `odoo_employee_id` والبريد الوظيفي والإدارة والمدير والموقع.
- عند الضغط على **تحديث من Odoo** يصبح سجل مالك المرحلة مرتبطًا بموظف Odoo الفعلي.

بهذا يمكن اختبار توزيع المسؤوليات على فريق حقيقي دون تلويث بيانات مشروعات Odoo بمشروعات وهمية.

## التفعيل

```powershell
git pull origin agent/task-scheduling-executive-management
npx --yes supabase@latest db push
npx --yes supabase@latest functions deploy platform-team --no-verify-jwt --project-ref svmjtmjcuetrfmpqqbpe --use-api
```

بعد ذلك:

1. سجّل الدخول من جديد للحصول على جلسة ARAAK CEO حديثة.
2. افتح **فريق المنصة** واضغط **تحديث من Odoo** لحفظ روابط الموظفين.
3. افتح **إدارة المهام والهندلة الزمنية**.
4. اختر أحد المسارات التي يبدأ عنوانها بـ **تجريبي**.
5. تنقّل بين المسار التنفيذي، مخطط Gantt، ولوحة المهام.

## إزالة البيانات التجريبية لاحقًا

شغّل الأوامر التالية من SQL Editor في Supabase بعد انتهاء الاختبار:

```sql
DELETE FROM public.workflow_instances WHERE is_demo = true;
DELETE FROM public.tenders WHERE is_demo = true;
DELETE FROM public.opportunities WHERE is_demo = true;
```

حذف `workflow_instances` يحذف المراحل والمهام ونقاط التسليم التجريبية المرتبطة بها تلقائيًا عبر علاقات `ON DELETE CASCADE`.
