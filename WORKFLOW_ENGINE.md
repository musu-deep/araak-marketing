# محرك الهندلة الزمنية للمنافسات والمشروعات

تحولت وحدة **إدارة المهام والتزمين** إلى مركز قيادة زمني يوضح رحلة ملف المنافسة من تاريخ الاستلام إلى تاريخ الرفع النهائي، ويكشف موضع التأخير والمسؤول عنه وأثره على موعد التسليم.

## المكونات الرئيسية

- مسار مرحلي ثلاثي الأبعاد بنسب تقدم مستقلة لكل مرحلة.
- مخطط Gantt يعرض الإدارات والمراحل مقابل نافذة التسليم.
- سجل انتقال الملف من إدارة إلى أخرى مع المخطط والمرسل والمستلم وزمن الانتظار.
- كشف تلقائي لموضع الاختناق والمراحل المتأخرة.
- حساب مرجّح للإنجاز الكلي بناءً على وزن كل مرحلة.
- إنشاء مهمة تلقائية لكل مرحلة وربط تحديث المهمة بتقدم المسار.
- قوالب قابلة للتوسعة حسب نوع المنافسة أو المشروع.

## قالب مشروع المباني

القالب الافتراضي `building-project-tender` يوزع كامل الفترة الزمنية على:

1. استلام الفرصة وتسجيل الملف.
2. الدراسة الأولية وقرار المشاركة.
3. الدراسة الفنية وتحليل الكراسة.
4. حصر البنود والكميات والمتطلبات.
5. طلب واستلام عروض الأسعار.
6. المقارنات وبناء مصفوفة التسعير.
7. مراجعة واعتماد مسؤول المشاريع.
8. إعادة الملف لمسؤول المنصة.
9. المراجعة النهائية والرفع.
10. هامش الأمان قبل الإغلاق.

لكل مرحلة مالك وظيفي، مخرج متوقع، وزن زمني، لون، حالة، نسبة إنجاز، بداية ونهاية مخططة وفعلية.

## جداول قاعدة البيانات

- `workflow_templates`
- `workflow_template_stages`
- `workflow_instances`
- `workflow_stage_instances`
- `workflow_handoffs`

كما أضيف إلى جدول `tasks`:

- `workflow_id`
- `workflow_stage_id`
- `task_type`

## الدوال والتزامن

### `create_competition_workflow`

تنشئ مسارًا كاملًا من القالب، وتوزع الفترة من الاستلام إلى الإغلاق حسب أوزان المراحل، وتعيّن المسؤولين، وتنشئ المهام ونقاط التسليم تلقائيًا.

### `refresh_workflow_progress`

تحسب الإنجاز الكلي المرجّح، المرحلة الحالية، وحالة الخطر عند تجاوز موعد أي مرحلة أو موعد التسليم النهائي.

### مزامنة المهام

أي تحديث لنسبة إنجاز مهمة مرحلية يحدّث المرحلة المقابلة، وعند إقفال المهمة يسجل انتقال الملف إلى المرحلة التالية.

## التفعيل

بعد سحب الفرع، طبّق Migration الجديدة:

```powershell
git pull origin agent/task-scheduling-executive-management
npx --yes supabase@latest db push
```

الملف المتوقع:

```text
20260728200000_add_competition_workflow_engine.sql
```

بعد نجاح التطبيق افتح المنصة ثم:

1. ادخل إلى **إدارة المهام والهندلة الزمنية**.
2. اضغط **هندلة مسار منافسة**.
3. اختر المنافسة أو الفرصة.
4. اختر قالب مشروع المباني.
5. حدد تاريخ الاستلام وموعد إغلاق التسليم.
6. أنشئ المسار؛ ستظهر المراحل والمهام وGantt ونقاط التسليم تلقائيًا.

## التحقق الآلي

أضيف Workflow باسم `Validate App` يقوم في كل تحديث بـ:

- تثبيت الحزم.
- فحص TypeScript.
- بناء Vite للإنتاج.
- حفظ سجلات الفحص كـArtifact عند الحاجة.
